<%= simple_form_for(@doctype) do |f| %>
  <% if @doctype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctype.errors.count, "error") %> prohibited this doctype from being saved:</h2>

      <ul>
      <% @doctype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :code %>
    <%= f.input :name %>
    <%= f.input :description %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
